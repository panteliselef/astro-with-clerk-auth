---

---

<script>
  import { $state } from "../lib/clerkJSInstance";
  console.log("mounted");

  $state.subscribe(({ user, session }) => {
    console.log("User", user);
    console.log("Session", session);
  });
</script>
